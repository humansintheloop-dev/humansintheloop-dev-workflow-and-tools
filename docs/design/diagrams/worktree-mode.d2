direction: right

**.style.font-size: 20

legend: "Claude Code" {
  icon: ../simpler-coding-agent-logo.png
  near: bottom-center
}

Plan: Plan with tasks {
  style.font-size: 28
  task: "" {
    shape: image
    icon: ../plan-task.png
    width: 300
  }
}

Main: "i2code implement" {
  style.font-size: 28
  direction: down

  Setup: "Setup:" {
    style.font-size: 24
    direction: right
    worktree: "Ensure Git worktree exists"
    recovery: "Commit recovery" {
      icon: ../simpler-coding-agent-logo.png
      icon.near: bottom-right
    }
    push: "Push commits"
    worktree -> recovery -> push
  }

  Loop: "For each incomplete task:" {
    style.font-size: 24
    direction: down
    fix-ci: "Fix CI failures" {
      icon: ../simpler-coding-agent-logo.png
      icon.near: bottom-right
    }
    comments: "Process PR comments" {
      icon: ../simpler-coding-agent-logo.png
      icon.near: bottom-right
    }
    implement: "Implement task and commit" {
      icon: ../simpler-coding-agent-logo.png
      icon.near: bottom-right
    }
    push-pr: "Push / Create PR"
    wait-ci: "Wait for CI"
    fix-ci -> comments -> implement -> push-pr -> wait-ci
  }

  ready: "Mark PR ready"

  Setup -> Loop
  Loop -> ready
}

PR: "GitHub Pull Request" {
  style.font-size: 28
  shape: rectangle
}

Plan -> Main -> PR
PR -> Main: "comments / reviews" {
  style.stroke-dash: 3
}
PR.near: Main
PR.left: 1800
PR.top: 300