Enhance `i2code go`

# Currently 

once the plan exist `i2code go` prompts to implement.

# New behavior

 Before doing so, if there are uncommitted changes (staged, unstaged, untracked) in the idea directory, `i2code go` should offer the option to commit them - default yes. 
 Then on the next iteration. `i2code go` should offer the  implement option  