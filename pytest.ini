[pytest]
markers =
    unit: Fast unit tests (no external dependencies)
    integration: Slower tests requiring git/GitHub
    integration_gh: Integration tests requiring GitHub CLI authentication
    manual: Tests that require manual invocation (e.g. real Claude calls)
    gather_fixtures: Tests that fetch real data from external services and write fixture files
