$SESSIONS_DIR is a directory that contains multiple Claude Code session transcripts: session-<timestamp>-<sessionID>.md

$ISSUES contains issue files that reference the session IDs.

IMPORTANT: Analyze the sessions in $SESSIONS_DIR and produce a summary report that includes:

* What was done
* Changes to the design that the user requested
* The corrections that the user requested
* The issues that were reported and their suggested fixes

Write the report to the $REPORT_FILE in adoc format.
